# Insight Editor Clean

A fresh Flutter implementation based on the recoverable UI concepts from the supplied APK. It intentionally has no access-code gate and stores edits locally.

## Build
1. Install Flutter.
2. Run `flutter pub get`.
3. Run `flutter build apk --release`.

The resulting APK is under `build/app/outputs/flutter-apk/`.
